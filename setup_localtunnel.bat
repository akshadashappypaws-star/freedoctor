# Install LocalTunnel globally
npm install -g localtunnel

# Start tunnel (this creates a permanent subdomain)
lt --port 8000 --subdomain freedoctor-webhook

# Your permanent URL will be: https://freedoctor-webhook.loca.lt
