🚀 HOSTINGER DEPLOYMENT - STEP BY STEP

## Method 1: SSH + GitHub Clone (Recommended)

### Step 1: Access Your Server
1. Open Terminal/Command Prompt
2. Connect: `ssh u150415685@in-mum-web1823.main-hosting.eu`
3. Enter your password when prompted

### Step 2: Deploy Application
```bash
# Navigate to domain folder
cd /home/u150415685/domains/freedoctor.in/public_html

# Clear existing files (if any)
rm -rf * .htaccess

# Clone your repository
git clone https://github.com/Mytechdata/freedoctor-development.git .

# Install dependencies
composer install --no-dev --optimize-autoloader

# Setup environment
cp .env.example .env
php artisan key:generate --force

# Set permissions
chmod -R 755 storage
chmod -R 755 bootstrap/cache
```

### Step 3: Configure Database
Edit the .env file:
```bash
nano .env
```

Update these lines:
```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://freedoctor.in

DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=u150415685_freedoctor
DB_USERNAME=u150415685_user
DB_PASSWORD=your_database_password
```

### Step 4: Run Migrations
```bash
php artisan migrate --force
php artisan config:cache
php artisan route:cache
```

### Step 5: Test Your Application
- Visit: https://freedoctor.in
- Test webhook: https://freedoctor.in/webhook/whatsapp?hub.mode=subscribe&hub.verify_token=FreeDoctor2025SecureToken&hub.challenge=test123

---

## Method 2: File Manager Upload (Alternative)

If SSH doesn't work:
1. Download `freedoctor-clean.zip` from your local machine
2. Login to Hostinger File Manager
3. Upload to `public_html/`
4. Extract the zip file
5. Follow steps 3-5 from Method 1

---

## ✅ Success Indicators

- ✅ Website loads at https://freedoctor.in
- ✅ Webhook test returns "test123"
- ✅ Database connection successful
- ✅ No error messages in browser

## 🔧 Troubleshooting

**500 Error**: Check file permissions and .env configuration
**Database Error**: Verify database credentials
**Webhook Issues**: Ensure domain is accessible publicly

---

Your FreeDoctorCORPO application is ready for deployment! 🎉
